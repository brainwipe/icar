
import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
  primary: { main: '#171b4a', contrastText: '#E8C547' },
  secondary: { main: '#B9BAC8', contrastText: '#000000' }
};
const themeName = 'Icar Standard';

export default createMuiTheme({ palette, themeName });